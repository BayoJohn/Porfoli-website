kind: pipeline
type: docker
name: deploy

clone:
  disable: true

steps:
  - name: clone-code
    image: alpine/git
    commands:
      - git clone http://172.17.0.1:3001/Bayo/Porfolio-website.git .
      - git checkout ${DRONE_COMMIT}

  - name: build-and-push
    image: plugins/docker
    settings:
      registry: 10.0.0.2
      repo: 10.0.0.2/library/portfolio
      tags:
        - latest
        - build-${DRONE_BUILD_NUMBER}
      username: admin
      password:
        from_secret: harbor_password
      dockerfile: Dockerfile
      insecure: true

  - name: update-k8s-manifest
    image: alpine/git
    environment:
      GITEA_TOKEN:
        from_secret: gitea_token
    commands:
      - git config --global user.email "drone@ci.local"
      - git config --global user.name "Drone CI"
      - git clone http://172.17.0.1:3001/Bayo/Porfolio-website.git /tmp/repo
      - sed -i "s|image:.*portfolio:.*|image: 10.0.0.2\/library\/portfolio:build-${DRONE_BUILD_NUMBER}|g" /tmp/repo/k8s/deployment.yaml
      - git -C /tmp/repo add k8s/deployment.yaml
      - git -C /tmp/repo commit -m "ci update image [skip ci]"
      - git -C /tmp/repo push http://Bayo:$GITEA_TOKEN@172.17.0.1:3001/Bayo/Porfolio-website.git
